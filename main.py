from facial_recognition.ui.app import start_app

if __name__ == "__main__":
    # name = input("Enter your name: ")
    # face = FaceData(name=name, data_path=uuid.uuid4(), created_at=datetime.now())
    # print(db.insert(face.model_dump()))
    start_app()
